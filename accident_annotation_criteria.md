# 交通事故视频标注要素判定标准

> 本文档是《交通事故视频素材结构化标注规范》（accident_annotation_spec_video.md）的配套细则，
> 对各待标注要素给出**可操作的判定标准**：什么情况判"是"、什么情况判"否"、
> 边界情况如何裁决。目标是让不同标注员对同一视频给出一致的标注结果。
>
> 视频时序属性的时间戳填写规则见第 9 节；字段结构见视频标注规范第 3 节。

---

## 目录

1. [总则](#1-总则)
2. [交通流类判定标准](#2-交通流类判定标准)
3. [停车行为类判定标准](#3-停车行为类判定标准)
4. [灯光类判定标准](#4-灯光类判定标准)
5. [碰撞证据类判定标准](#5-碰撞证据类判定标准)
6. [行为类判定标准](#6-行为类判定标准)
7. [环境类判定标准](#7-环境类判定标准)
8. [场景归类边界](#8-场景归类边界)
9. [事件时间定位规则](#9-事件时间定位规则)
10. [参与者角色判定](#10-参与者角色判定)
11. [accident 最终判定决策树](#11-accident-最终判定决策树)
12. [confidence 判定标准](#12-confidence-判定标准)
13. [常见边界案例裁决表](#13-常见边界案例裁决表)

---

## 1. 总则

### 1.1 度量基准

监控视频无法直接测速测距，所有标准使用**画面内可观察的代理量**：

| 代理量 | 定义 |
|---|---|
| 车身长 | 以画面中目标车辆自身长度为距离单位（约 4~5 米） |
| 静止 | 目标在连续帧中位置无可辨识移动 |
| 正常速度 | 与该路段同时段大多数车辆的通行速度一致 |

### 1.2 判定优先级

1. **过程优先于状态**：能看到"怎么停下来的"，优先按过程判；只能看到"停着"，按状态+周边反应判；
2. **多数帧原则**：状态类字段（如 traffic_flow）按视频中**占时最长**的状态标注；若前后半段状态截然不同（如事故导致由畅通转中断），按第 9 节事件时间定位拆分描述，traffic_flow 标事件发生后的主导状态；
3. **就近原则**：与事件区域相关的字段（如 bypass_behavior）只看事件区域附近，不看画面远端。

### 1.3 "不确定"的使用规则

同时满足以下两条才允许标"不确定"：

```text
① 正常速度完整看过一遍 + 关键段逐帧回看过；
② 两种结论都有画面依据支持，或关键区域完全不可辨识。
```

禁止因"懒得逐帧看"而标不确定。单个样本中"不确定"字段超过 4 个时，该样本转专家标注。

---

## 2. 交通流类判定标准

### 2.1 `traffic_flow` 五级判定

按下列顺序依次检查，命中即停：

#### ① 某点中断

**判定标准**（三条同时满足）：

```text
a. 画面中存在一个固定位置，车辆到达该位置后停止或绕行；
b. 该位置下游明显稀疏或空置（车流"断层"）；
c. 该位置上游出现积压或减速。
```

**反例（不算某点中断）**：
- 信号灯停止线处的排队（下游空是因为红灯，属"停止排队"）；
- 收费站、检查站前的排队（常态设施）。

#### ② 停止排队

**判定标准**：

```text
画面中多数车辆完全静止 ≥ 10 秒，且呈首尾相接的队列形态。
```

#### ③ 拥堵

**判定标准**：

```text
多数车辆呈"走走停停"状态：
10 秒内位移 ≤ 3 个车身长，且单次静止一般 < 10 秒。
```

#### ④ 缓行

**判定标准**：

```text
多数车辆持续移动、无长时间静止，
但速度明显低于该路段正常速度（10 秒内位移约 3~10 个车身长）。
```

#### ⑤ 畅通

**判定标准**：

```text
多数车辆以正常速度通过画面，无成队减速。
```

### 2.2 `congestion` 汇总字段

```text
traffic_flow ∈ {拥堵, 停止排队} → congestion = 是
traffic_flow ∈ {畅通, 缓行}     → congestion = 否
traffic_flow = 某点中断         → congestion 按中断点上游状态判定
```

### 2.3 `flow_interruption_point`

与 2.1① 相同标准，单独作为字段是因为：**整体拥堵中也可能叠加事故中断点**。
拥堵路段中若某一位置的静止车辆在周围车流恢复移动后仍持续不动、且被绕行 → 标"是"。

### 2.4 `queue_at_signal`（路口专用）

```text
判"是"：车辆静止位置在停止线后方、队列头部对齐停止线，
        且（信号灯可见时）静止时段对应红灯相位，绿灯后队列正常起步。
判"否"：绿灯期间仍不动的车辆、队列中间断裂处静止的车辆（转 abnormal_stop 检查）。
```

---

## 3. 停车行为类判定标准

### 3.1 `abnormal_stop`（异常停止）

**判"是"（满足任一条）**：

```text
a. 停止位置在行车道中间，且前方无信号灯、无排队队列、无前车阻挡；
b. 车身斜停：与车道线夹角目测 > 15°；
c. 车身横跨两条车道线停止；
d. 在移动车流中突然静止（周围车辆持续移动、只有它不动 ≥ 5 秒）；
e. 隧道/高速行车道内的任何静止（这两类场景行车道停车本身即异常）。
```

**判"否"**：

```text
排队/等灯静止；应急车道或路边贴边静止且车身端正；公交车在站台停靠。
```

### 3.2 `smooth_pullover`（平稳靠边临停）

**判"是"（三条同时满足）**：

```text
a. 减速过程平缓：从减速到静止跨越 ≥ 3 秒，无急刹点头、无方向突变；
b. 停止位置合理：应急车道、路肩、路边、港湾式车位；
c. 停止后车身端正：与道路方向平行，不占用行车道。
```

任何一条不满足 → 判"否"并检查 `abnormal_stop`。

### 3.3 `stop_position`

```text
行车道：  车身任一部分压在行车道内（含骑压车道线）
应急车道：车身完整位于应急车道/硬路肩内
路边：    城市道路贴边、港湾、划线车位
无停车：  视频中无静止目标车辆
```

### 3.4 等灯排队 vs 事故停车的对照判别

| 观察点 | 等灯排队 | 事故停车 |
|---|---|---|
| 停止位置 | 停止线后、队列内 | 队列外、路口中央、车道中间 |
| 队形 | 首尾对齐、间距均匀 | 角度散乱、间距异常 |
| 恢复 | 绿灯后依次起步 | 周围恢复通行后仍静止 |
| 周围反应 | 无绕行 | 后车变道绕行 |

---

## 4. 灯光类判定标准

### 4.1 `hazard_light`（双闪判定）

**必须逐帧/慢放确认闪烁**，单帧禁止判定双闪。

**判"是"**：

```text
车辆左右两侧转向灯以固定周期（约 0.5~1 秒一闪）同步闪烁。
```

**与相似灯光的区分**：

| 灯光 | 特征 | 判定 |
|---|---|---|
| 双闪 | 左右同步、周期性亮灭 | hazard_light = 是 |
| 刹车灯 | 减速时亮起、**持续常亮**、位置居中偏上（高位刹车灯） | 否 |
| 转向灯 | 仅一侧周期闪烁 | 否 |
| 夜间灯光反射 | 随车辆/摄像头角度变化，无固定周期 | 否，并置 glare_or_reflection = 是 |

夜间过曝导致无法分辨左右是否同步 → "不确定"。

### 4.2 `hazard_light_reason` 决策树

```text
车辆开双闪
   │
   ├─ 车辆随车流持续移动？
   │     └─ 是 → 【拥堵缓行】（含隧道双闪链）
   │
   ├─ 车辆静止，停止过程可见？
   │     ├─ 平稳靠边（满足 smooth_pullover）→ 【临时停车】
   │     ├─ 急停/偏转/碰撞后停止 → 【事故后】
   │     └─ 停在施工区/有三角牌/引擎盖打开 → 【故障施工】
   │
   ├─ 车辆静止，停止过程不可见？
   │     ├─ 车身端正、位置合理（应急车道/路边）→ 【临时停车】（confidence 降为中）
   │     ├─ 姿态歪斜/行车道中间/有其他事故证据 → 【事故后】
   │     └─ 无其他任何证据 → 【不明确】
   │
   └─ 无法判断 → 【不明确】
```

### 4.3 `hazard_light_chain`（隧道双闪链）

**判"是"**：

```text
拥堵/缓行车流中，≥ 3 辆车先后开启双闪，
且这些车辆均随车流移动、无静止、无碰撞证据。
```

这是隧道场景最高频的误报源，判定为"是"时该样本自动 `hard=true`。

---

## 5. 碰撞证据类判定标准

### 5.1 `collision_visible`（碰撞可见）

**判"是"（满足任一条）**：

```text
a. 直接看到两目标接触瞬间；
b. 看到接触导致的运动突变：弹开、车身明显晃动、一方被推移、
   行人/骑车人接触后倒地或改变运动轨迹。
```

**判"否"的典型**：

```text
两车在画面中重叠（透视遮挡）但随后正常分开、速度无突变；
远景中两车"贴近"但像素不足以确认接触 → 标"不确定"。
```

### 5.2 `collision_occluded`（碰撞被遮挡）

**判"是"（两条同时满足）**：

```text
a. 依据前后状态可推断发生了接触（接近 → 遮挡区/盲区 → 出现事故后状态）；
b. 碰撞瞬间因遮挡/盲区/画质不可见。
```

注意：`collision_visible=是` 时本字段必为"否"，两者互斥。

### 5.3 `abrupt_trajectory_change`（轨迹突变）

**判"是"**：

```text
目标在 1 秒内出现以下任一变化，且无正常驾驶解释（避让障碍不算突变来源限制）：
急偏转（行进方向瞬间改变 > 30°）/ 旋转打转 / 被弹开 / 突然翻滚。
```

**判"否"**：正常变道、正常转弯、平缓避让。

### 5.4 `posture_anomaly`（姿态异常）

**判"是"（满足任一条）**：

```text
车头方向与车道方向夹角目测 > 30°；
车辆骑压路缘石/护栏；侧翻/四轮离地；
两车以非正常间距贴合静止（车头贴车尾无间隙）。
```

### 5.5 `debris_scatter` / `debris_on_road`

**判"是"**：

```text
事件时刻前后路面新出现的散落物：碎片、保险杠、玻璃反光点、大面积液体痕迹。
```

**判"否"**：视频开始时就存在的静态杂物（无法确认与本事件相关）→ 高速场景可标 `debris_on_road=是` 但不作为本次事故证据。

### 5.6 `person_down`（人员倒地）

**判"是"**：

```text
行人/骑车人身体接触地面且非主动蹲坐（倒地姿态：平躺/侧卧/翻滚后趴伏），
或倒地后周围人员聚集查看。
```

**判"否"**：蹲下检查车辆、坐在路缘休息。远景无法分辨 → "不确定"。

### 5.7 `vehicle_deformation`（车损可见）

**判"是"**：能明确看到凹陷、部件脱落、车灯破碎等。
监控分辨率通常看不清车损，**远景一律标"不确定"而非"否"**（避免把"看不见"当"没有"）。

---

## 6. 行为类判定标准

### 6.1 `bypass_behavior`（绕行）

**判"是"（两条同时满足）**：

```text
a. ≥ 2 辆后续车辆在同一固定位置前变道，绕过同一目标/区域；
b. 绕行后恢复原车道或正常行驶。
```

**判"否"**：单车偶发变道；因施工锥桶导向的绕行（标 construction_zone）。

### 6.2 `near_miss`（险情未碰撞）

**判"是"（三条同时满足）**：

```text
a. 两目标最小间距目测 < 1 个车身长；
b. 至少一方出现明显应急动作：急刹（车头下沉/急停）、急打方向；
c. 随后正常分开，双方均无停止、无接触后果。
```

### 6.3 `close_distance_pass`（近距离通过）

```text
两目标近距离（< 1 车身）交会/跟随，但无应急动作、无接触 → 判"是"。
与 near_miss 的区别：有没有"应急动作"。
```

### 6.4 `people_exit_vehicle`（人员下车）

**判"是"**：静止车辆有人下车，并走向车辆前后方查看或走向另一目标。
**判"否"**：正常停靠上下客（公交站、路边即停即走）。

### 6.5 `pedestrian_gathering`（人员聚集）

**判"是"**：≥ 2 人在车行道内某固定点停留 ≥ 5 秒（非过街等待）。

---

## 7. 环境类判定标准

### 7.1 `lighting`

```text
白天：    自然光充足，无需路灯即可辨识细节
夜间：    依赖路灯/车灯照明
黄昏黎明：自然光不足但天空有亮度，路灯与天光并存
隧道暗光：隧道内人工照明段
```

### 7.2 `visibility`

以"能否辨识关键判别要素"为标准，不以主观清晰度：

```text
高：能辨识单个车辆的灯光状态和车身姿态
中：能辨识车辆轮廓和运动，但灯光/姿态细节勉强可辨
低：只能辨识车流整体运动，无法辨识单车细节
```

### 7.3 `weather` / `road_surface`

```text
雨：画面有雨丝/雨刷动作/路面反光水痕，三者任一即可判
湿滑：路面呈镜面反光或明显水渍
积水积雪：可见成片积水/积雪覆盖
无法判断时 → 不确定（夜间路面反光易与湿滑混淆，需结合车辆溅水判断）
```

### 7.4 `glare_or_reflection`

**判"是"**：画面存在持续或间歇的强光源过曝区域（对向车灯、路灯反射、隧道口逆光），且该区域覆盖或邻近关键判别区。

---

## 8. 场景归类边界

### 8.1 `scene` 五类归属规则

```text
高速高架：  封闭道路、无信号灯、有护栏/隔离带；城市高架归此类
城市路口：  画面主体包含交叉口冲突区（两条道路交汇区域）
城市普通路段：非封闭城市道路且画面主体不含交叉口冲突区
隧道：      画面主体在隧道结构内（含隧道口过渡段）
匝道收费站：匝道弯道、收费广场、出入口渐变段
```

**边界裁决**：

| 情况 | 归属 |
|---|---|
| 画面同时含路口和路段 | 事件区域在哪就归哪；事件区域不明确按画面占比 |
| 高架下的地面路口 | 按摄像头拍摄主体（拍高架归高速高架，拍地面归路口） |
| 隧道口外 50 米内 | 归隧道（入口段/出口段） |
| 高速服务区出入口 | 归匝道收费站 |

### 8.2 `tunnel_zone`

```text
入口段：画面可见隧道口亮光（从外向内）
出口段：画面可见隧道口亮光（从内向外）
中段：  画面全为人工照明，无洞口
```

---

## 9. 事件时间定位规则

> 配套视频标注规范 v6：事件级纯时间字段 + 时序属性 `{value, start_sec, end_sec[, bbox]}`。
> 时间原点为视频起点，单位秒，精度 0.1s；位置相关属性另标 `bbox=[x,y,w,h]`。

### 9.1 `event_start_sec`

```text
事故正样本：取以下最早出现者——
  碰撞接触瞬间 / 首个应急动作（急刹、急转）/ 首个异常行为（失控、偏离）
困难负样本：可疑行为开始时刻（如双闪首次出现、排队形成）
普通负样本：填 0
```

### 9.2 `event_end_sec`

```text
状态稳定时刻：所有参与者静止且位置不再变化，
或参与者驶离画面，或视频结束（取先到者）。
普通负样本：填视频时长 duration_sec
```

### 9.3 `collision_moment_start_sec` / `collision_moment_end_sec`

```text
碰撞接触时间窗（两目标发生接触/撞击的起止）。
填写条件：collision_moment_visible = 是 → 二者必填；
         = 否 / 无碰撞 → 必须为 null。
通常 start 与 evidence.collision_visible.start_sec 相差 ≤ 0.2s；
瞬时碰撞可令 end = start 或 start+0.2。
```

### 9.4 时序属性 `start_sec` / `end_sec` 定位通则

```text
① start：首次可确认出现；end：现象结束/离画/稳定；瞬时可 start=end；
② value=是 → start/end 必填；value∈{否,不适用,未标注} → 均为 null；
③ 预警类（sudden_brake_wave）可早于 event_start_sec；
   后果类通常 ≥ event_start_sec；
④ 帧号换算：sec = round(frame_idx / fps, 1)；
⑤ 位置相关属性同时填写 bbox=[x,y,w,h]（画面左上角坐标系）。
```

### 9.5 `event_stage_coverage`

```text
全过程：  start 前 ≥ 2 秒正常状态可见，且碰撞/异常瞬间可见，且 end 后状态可见
仅前+后：事件前后可见，但碰撞瞬间被遮挡/盲区（对应 collision_occluded=是）
仅后：    视频开始时事故已发生（只能看到事故后状态）
仅前：    视频在碰撞前结束（此类样本不得标 accident=是，除非另有依据）
```

---

## 10. 参与者角色判定

```text
主动方：  运动状态主动改变并导致接触的一方
          （追尾中的后车、变道剐蹭中的变道车、闯灯方）
被动方：  被接触时处于正常行驶/静止状态的一方
受波及：  未直接参与初次碰撞、但被卷入后续的目标（连环追尾的后续车辆）
不确定：  双方同时变道/无法确定先后 → 双方均标不确定
```

注意：角色判定仅用于数据分析，**不代表责任认定**，标注员不做责任判断。

---

## 11. accident 最终判定决策树

```text
开始
 │
 ├─ 直接证据检查：collision_visible / person_down / rollover / vehicle_deformation
 │   任一为"是"？
 │     └─ 是 → accident = 是（confidence = 高）
 │
 ├─ 间接证据检查：collision_occluded = 是？
 │     └─ 是 → 统计以下证据为"是"的数量：
 │             abnormal_stop / abrupt_trajectory_change / posture_anomaly /
 │             bypass_behavior / flow_interruption_point / people_exit_vehicle
 │           ├─ ≥ 2 项 → accident = 是（confidence = 中）
 │           └─ ≤ 1 项 → accident = 不确定（不入训练集）
 │
 ├─ 误报要素检查：
 │   congestion_only=是 / smooth_pullover=是 / near_miss=是 /
 │   close_distance_pass=是 / queue_at_signal=是 / hazard_light_chain=是
 │   且无任何碰撞证据？
 │     └─ 是 → accident = 否（按 hard 派生规则打标）
 │
 └─ 以上均不满足、画面无任何异常 → accident = 0（普通负样本）
```

---

## 12. confidence 判定标准

```text
高：直接证据可见，或全过程可见且证据链完整
中：依赖间接证据推断（遮挡型事故）；或关键证据仅部分可见
低：证据相互矛盾，或关键区域可见性差但倾向某一结论
    （confidence=低 的样本不进入 GRPO 训练集，SFT 中降权或复核）
```

---

## 13. 常见边界案例裁决表

| # | 案例 | 裁决 |
|---|---|---|
| 1 | 车辆停在行车道，双闪，无碰撞过程，2 分钟无人下车 | abnormal_stop=是；无其他证据时 accident=不确定，转专家 |
| 2 | 隧道内 5 辆车先后开双闪、缓慢移动 | hazard_light_chain=是，accident=否，hard=true |
| 3 | 两车距离极近通过，后车急刹，随后正常行驶 | near_miss=是，accident=否，hard=true |
| 4 | 视频开始时两车已贴在一起静止、有人查看 | collision_visible=否，事故后强证据（贴合静止+人员查看）→ accident=是，event_stage_coverage=仅后 |
| 5 | 路口绿灯亮后一辆车不动，后车绕行 | abnormal_stop=是，queue_at_signal=否；无碰撞证据 → accident=不确定 |
| 6 | 电动车自行摔倒，无与任何目标接触 | 单方事故：person_down=是 → accident=是，accident_type=撞行人非机动车不适用时选"其他/不确定"，note 注明"自行摔倒" |
| 7 | 货车遮挡区域，小轿车进入后未再出现，货车停下开双闪 | collision_occluded=是，检查间接证据数量后按决策树判定 |
| 8 | 雨夜路面反光，疑似双闪但无法确认闪烁周期 | hazard_light=不确定，glare_or_reflection=是 |
| 9 | 施工区锥桶导致所有车辆变道 | construction_zone=是，bypass_behavior=否（属导向绕行） |
| 10 | 收费站前排队 2 分钟不动 | toll_queue=是，traffic_flow=停止排队，accident=否 |
| 11 | 两车并排违停在路边聊天 | double_parked=是，smooth_pullover=是，accident=否 |
| 12 | 碰撞发生在画面边缘，只看到半个车身晃动 | 晃动可辨识 → collision_visible=是，confidence=中；不可辨识 → collision_occluded=是 + 走间接证据 |

---

## 附：与标注规范文档的关系

```text
accident_annotation_spec.md      → 标什么（字段定义、JSON 结构、流程）
accident_annotation_criteria.md  → 怎么判（本文档：每个字段的判定标准）
```

两份文档配套使用；标注培训时以本文档的判定标准 + 裁决表为考核内容。
裁决表随标注争议持续扩充：每次双人标注不一致经仲裁后，新结论回写为新案例。

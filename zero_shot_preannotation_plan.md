# 零样本预标注方案（交通场景图片 / 视频）

> 目标：在**没有任何已训练模型**的情况下，用零样本（zero-shot）模型批量生成标注草稿——
> 目标框（机动车/非机动车/行人/信号灯等）+ 现象字段（侧翻/起火等）+ 事件字段（事故/拥堵等），
> 人工只做**复核修正**，不做从零标注。产出直接对齐本仓库标注规范
> `accident_annotation_spec_image.md` / `accident_annotation_spec_video.md` 的 JSON 结构。

---

## 0. 总体架构：两层预标注

| 层 | 预标注内容 | 零样本模型 | 对应规范字段 |
|---|---|---|---|
| **检测层** | 目标框 + 类别 | Rex-Omni（主推）/ Grounding DINO / YOLO-World | `participants[].roi_box`、`accident_area_box`、属性中的 `bbox` |
| **认知层** | 现象、事件、场景要素等字段草稿 | Qwen3-VL-Instruct 原版（不微调，大杯优先） | `meta.scene`、现象/证据属性、事件分类、时间窗 |

两层独立运行、结果合并：检测层给"框"，认知层给"字段值"；检测结果注入认知层 prompt 可显著提高字段准确率（与 `accident_model_pipeline_design.md` 的级联思想一致）。

---

## 1. 检测层：零样本开放词汇检测选型

| 模型 | 形态 | 优势 | 局限 | 推荐场景 |
|---|---|---|---|---|
| **Rex-Omni**（IDEA，3B MLLM） | 开源权重，官方 `rex_omni` 包，transformers / vLLM 后端 | 检测精度高（COCO/LVIS 零样本接近闭集检测器）；支持任意短语类别；额外支持 pointing / OCR / 关键点 | 3B 自回归模型，单帧推理慢于 CNN 检测器；需要 GPU | **预标注主力**（离线批量，质量优先） |
| **Grounding DINO 1.0**（开源） / **MM-Grounding-DINO** | 开源权重，官方仓库或 MMDetection | 成熟稳定、社区资料多；回归式框更准 | 对长尾/罕见短语（"侧翻车辆"）召回一般；需要调文本阈值 | 预标注备选；与 Rex-Omni 交叉验证 |
| **DINO-X / Grounding DINO 1.5**（API） | 商用 API | 精度最强、免部署 | 按量计费；数据需外发，监控数据可能有合规问题 | 数据合规允许时的小批量高质量标注 |
| **YOLO-World** | 开源权重，Ultralytics 生态 | 推理极快（实时级），批量吞吐大 | 精度低于上面三者，小目标弱 | 超大规模粗筛（百万帧级第一遍） |

**推荐组合**：`Rex-Omni（主） + MM-Grounding-DINO（交叉验证）`。两个模型都检出的框置信度标高，只有一个检出的标低、进人工重点复核队列。

### 1.1 类别提示词映射表（英文提示 → 规范类别）

零样本检测器用英文短语召回更好，检出后映射回规范里的中文类别：

| 检测提示词（prompt） | 规范类别 |
|---|---|
| car, truck, bus, van, trailer | 机动车（细分：轿车/货车/客车…） |
| motorcycle, bicycle, electric scooter, tricycle | 非机动车 |
| pedestrian, person | 行人 |
| traffic light | 信号灯 |
| traffic cone, warning triangle | 锥桶/三角牌（施工与故障证据） |
| overturned vehicle, vehicle on its side | 侧翻车辆（现象证据框） |
| fire, flame, smoke | 起火/冒烟（现象证据框） |
| scattered cargo, debris on road, broken glass | 散落物/碎片（事故证据框） |
| person lying on the road | 人员倒地（现象证据框） |

要点：

- 一次推理传入**全部提示词**（Rex-Omni 的 `categories` 参数 / Grounding DINO 的 caption 拼接），不必逐类跑；
- "现象证据框"（侧翻/火焰/散落物）同时服务检测层和认知层：检出即为认知层字段的强先验；
- 坐标统一转成规范约定的 `[x, y, w, h]` 绝对像素（Rex-Omni 输出 `[x0, y0, x1, y1]` 需转换）。

---

## 2. 认知层：零样本 VLM 字段草稿

用**未微调**的 Qwen3-VL-Instruct（预标注阶段建议用大杯 30B-A3B / 235B-A22B，质量优先；显存有限用 8B）+ schema 约束 prompt，直接产出规范 JSON 的字段草稿。ms-swift 可用于该模型的批量推理与后续部署（`swift infer` / vLLM backend）。

### 2.1 Prompt 模板（图片）

```text
你是交通监控标注助手。已由检测器检出以下目标（坐标为[x,y,w,h]像素）：
机动车×6: [[120,340,80,60], ...]；行人×2: [[560,300,30,80], ...]；
信号灯×1（位于[900,50,20,60]）；疑似散落物×1: [[430,500,60,30]]。

请观察图片并按以下 JSON 输出（不确定的字段填"不确定"，不要编造）：
{
  "scene": "<高速高架/城市路口/城市路段/隧道/收费站/匝道>",
  "phenomena": [{"value": "<侧翻/起火/冒烟/人员倒地/散落物/车体变形/无>", "bbox": [x,y,w,h] 或 null}],
  "event": "<交通事故/交通拥堵/车辆故障/施工占道/正常通行/不确定>",
  "evidence": "<一句话判定依据，引用画面可见证据>",
  "confidence": "<高/中/低>"
}
```

### 2.2 关键做法

1. **检测框注入**：认知层 prompt 里带上检测层结果（如上），治理 VLM 数错车、漏看小目标；
2. **schema 约束 + 枚举值**：所有字段给死枚举选项，禁止自由发挥，便于程序解析和人工复核；
3. **"不确定"出口**：明确允许模型输出"不确定"，比强行猜更利于后续人工分流；
4. **自一致性投票（可选，质量再提一档）**：同一图片跑 3 次（temperature 0.7），字段值取多数票，三次不一致 → 低置信、进人工队列；
5. **困难负样本重点覆盖**：拥堵、双闪故障车、遮挡场景的预标注**必须全量人工复核**——这正是历史误报来源，零样本模型同样容易在这里犯错。

---

## 3. 图片预标注流水线

```
原始图片库
   │
   ├─► ① Rex-Omni 零样本检测（vLLM 批量）──► 框草稿 [{label, bbox, conf}]
   │        │（可选）MM-Grounding-DINO 二检 → 交叉验证定置信度
   │        ▼
   ├─► ② Qwen3-VL 零样本字段草稿（检测框注入 prompt）──► 字段草稿 JSON
   │        ▼
   ├─► ③ 合并脚本：框 + 字段 → 规范 JSON（bbox 转 [x,y,w,h]；置信度分档）
   │        ▼
   └─► ④ Label Studio 导入预标注 → 人工复核修正 → 终版标注 JSON
```

**置信度分档规则（③ 中实现）：**

| 档位 | 条件 | 人工处理方式 |
|---|---|---|
| 高 | 双检测器一致 且 VLM confidence=高 且 event=正常通行 | 抽检 10% |
| 中 | 单检测器检出 或 VLM confidence=中 | 全量快速复核 |
| 低 | 检测/VLM 冲突、confidence=低、event∈{事故,不确定}、困难负样本场景 | 全量精细复核 |

**正样本（事故）与困难负样本（拥堵/双闪/故障）无论置信度一律全量复核**——它们是训练价值最高的数据，也是零样本模型最易出错的数据。

---

## 4. 视频预标注流水线

视频比图片多两件事：**逐帧框的时序关联（track_id）** 和 **时间窗草稿（start_sec/end_sec）**。

```
原始视频
   │
   ├─► ① 抽帧：事件粗定位用 1 fps；确认事故片段后加密到 2–5 fps
   │        ▼
   ├─► ② Rex-Omni 逐帧零样本检测 ──► 逐帧框
   │        ▼
   ├─► ③ ByteTrack 关联（只需检测框输入，无需训练）──► track_id + 轨迹
   │        ▼
   ├─► ④ 轨迹统计生成时序摘要：急减速/停驶起止/轨迹交叉时刻
   │        ▼
   ├─► ⑤ Qwen3-VL 视频零样本理解（抽帧序列 + 轨迹摘要注入）
   │      ──► 事件分类 + event_start_sec/event_end_sec + collision_moment 窗草稿
   │      ──► 时序现象草稿 {value, start_sec, end_sec, bbox}
   │        ▼
   ├─► ⑥ 合并脚本 → 视频规范 JSON 草稿
   │        ▼
   └─► ⑦ 人工复核：重点核对时间窗边界（±1s 精度）与碰撞瞬间窗
```

**要点：**

- ③ ByteTrack 是纯运动关联算法，**不需要训练**，直接吃零样本检测框即可产出轨迹——整条流水线保持零样本；
- ⑤ 时间窗草稿的产生方式：VLM 对"每 N 秒一个窗口"做滑窗判定，再取连续异常窗口的并集作为 `event_start_sec/event_end_sec`；碰撞瞬间窗由轨迹交叉时刻（④）给初值，VLM 确认；
- 人工复核的重心从"画框"转移到"校时间窗边界"，这是视频预标注省人力的主要来源；
- 长视频先用 ① 1fps + ⑤ 粗判把"无事件片段"整段排除，只对异常片段做 ②–⑥ 全流程。

---

## 5. 人工复核工具对接

| 工具 | 适用 | 预标注导入方式 |
|---|---|---|
| **Label Studio**（推荐） | 图片框 + 分类字段 + 视频时间轴 | 预标注 JSON 转 Label Studio `predictions` 格式导入，复核界面直接改 |
| CVAT | 框/轨迹精修（插值补帧强） | 转 CVAT XML / COCO 导入 |
| 自研网页 | 字段类复核（规范 JSON 字段多） | 直接渲染规范 JSON 表单 |

建议：**框和轨迹**在 Label Studio/CVAT 修，**字段值**用规范 JSON 表单修，两者靠样本 ID 关联。

---

## 6. 与本仓库资产的关系

1. 预标注产出 = 规范 JSON 草稿 → 人工复核后成为**唯一真源**，再派生任务 A–E 的 QA 训练数据（喂 ms-swift 微调 Qwen3-VL）和 COCO/YOLO 检测数据（喂 Ultralytics 训练闭集检测器）；
2. 零样本阶段结束后进入**自举循环**：用第一批复核数据微调出的 Qwen3-VL / YOLO 替换零样本模型做第二轮预标注，精度和吞吐都会显著超过零样本，人工复核成本进一步下降；
3. Rex-Omni / Grounding DINO **不进 ms-swift、不参与训练**，只在预标注离线工具链中使用（`rex_omni` 官方包独立部署，vLLM 后端批量推理）；
4. 认知层的 Qwen3-VL 原版推理可直接用 ms-swift 的 `swift infer`（vLLM backend）批量执行。

## 7. 落地步骤

1. **环境搭建**：部署 Rex-Omni（vLLM）+ Qwen3-VL（ms-swift/vLLM）；准备类别映射表与 prompt 模板；
2. **小批试跑**：抽 200 张图（含事故正样本 + 拥堵/双闪困难负样本）跑全流程，人工全量核对，统计各字段预标注准确率，迭代提示词与阈值；
3. **批量图片预标注**：按第 3 节流水线 + 置信度分档铺量；
4. **视频预标注**：按第 4 节流水线处理视频库，重点产出时间窗草稿；
5. **进入自举循环**：首批复核数据训练出专用模型后，替换零样本模型做后续轮次。

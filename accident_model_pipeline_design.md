# 交通场景感知与事件判定：模型组合使用方案

> 需求：给定一张交通场景图片（或一段视频），完成三级任务：
>
> 1. **目标标注**：检出机动车、非机动车、行人、信号灯等目标（bbox + 类别）；
> 2. **现象分析**：识别画面中的异常现象（车辆侧翻、起火、冒烟、散落物、人员倒地等）；
> 3. **事件判定**：判断发生了何种事件（交通事故 / 交通拥堵 / 正常通行 / 施工占道等）。
>
> 本文给出两套方案：**图片输入方案** 与 **视频输入方案**，并说明各模型的训练工具链与数据对接方式（与本仓库的标注规范 `accident_annotation_spec_*.md` 对齐）。

---

## 0. 总体设计思想：感知层 + 认知层分离

三级任务的性质不同，不建议用一个模型硬扛全部：

| 任务 | 性质 | 最适合的模型类型 |
|---|---|---|
| 1. 目标标注 | 密集检测，要求高召回、低延迟、坐标准 | 专用检测器（YOLO / RT-DETR / Grounding DINO / Rex-Omni） |
| 2. 现象分析 | 细粒度语义识别（侧翻姿态、火焰烟雾） | 视觉语言大模型（VLM，如 Qwen3-VL） |
| 3. 事件判定 | 综合推理（区分"事故"与"拥堵"需要理解因果与场景） | 视觉语言大模型（VLM） |

核心原则：

- **感知层（检测器）**负责"画框"：快、准、可在路侧边缘设备实时运行；
- **认知层（VLM）**负责"理解"：接收原图 + 感知层的检测结果，输出现象与事件的结构化结论；
- 检测结果**注入 VLM 的 prompt**（文本坐标或画框图），显著降低 VLM 漏检小目标、数错车辆数的问题；
- VLM 用本仓库的多任务标注数据（任务 A–E）微调，天然输出与标注规范一致的 JSON。

---

## 1. 图片输入方案

### 1.1 推荐方案（两阶段级联：检测器 + Qwen3-VL）

```
                      ┌────────────────────────────────────┐
 图片 ──┬───────────► │ 阶段一：目标检测器                   │
        │             │ YOLO11 / RT-DETR（闭集微调）         │──► 任务1输出：
        │             │ 类别：机动车/非机动车/行人/信号灯/     │    [{label, bbox, conf}, ...]
        │             │      锥桶/散落物/火焰烟雾(可选)       │
        │             └──────────────┬─────────────────────┘
        │                            │ 检测结果(bbox列表)
        ▼                            ▼
   ┌─────────────────────────────────────────────┐
   │ 阶段二：Qwen3-VL-Instruct（ms-swift 微调）     │
   │ 输入：原图 + 检测结果注入prompt                │──► 任务2输出：现象列表
   │ 多任务头：任务A(属性) / B(证据) / D(推理)      │    （侧翻/起火/冒烟/倒地/散落…）
   │                                             │──► 任务3输出：事件分类 + 依据
   └─────────────────────────────────────────────┘    （事故/拥堵/正常/施工）
```

**各组件选型与训练：**

| 组件 | 推荐模型 | 训练工具链 | 数据来源 |
|---|---|---|---|
| 目标检测器 | YOLO11-m/l 或 RT-DETR | Ultralytics / MMDetection（**不在 ms-swift 内**） | 监控图片 + bbox 标注（可用 Grounding DINO / Rex-Omni 预标注，人工复核） |
| 现象+事件 VLM | Qwen3-VL-4B/8B-Instruct | **ms-swift**（LoRA SFT，本仓库 `accident_detection_training_plan.md` 的任务 A/B/D/E） | 本仓库标注规范 JSON → 派生 QA |

**检测结果注入 VLM 的方式（二选一或并用）：**

1. **文本注入**：把检测框以文本写入 prompt，例如：

```text
已检测到目标：机动车×6 [[120,340,80,60], ...]，行人×2 [[560,300,30,80], ...]，信号灯×1（红）。
请结合图片分析：1) 是否存在侧翻/起火/冒烟/散落物/人员倒地；2) 判断事件类型（事故/拥堵/正常/施工）并给出依据。
```

2. **可视化注入**：把 bbox 画到图上再送入 VLM（对小目标计数更稳）。

**为什么"事故 vs 拥堵"要靠 VLM 而不是检测器：** 两者在检测层特征几乎相同（都是多车密集停驶）。区分依据是**碰撞证据**（车体变形、碎片、散落物、异常朝向、人员下车查看）——这正是本仓库任务 B（证据提取）与任务 D（对比推理）微调要解决的问题，也是历史误报（拥堵误报为事故）的治理点。

### 1.2 简化方案（单模型：仅 Qwen3-VL）

只用一个 Qwen3-VL 同时完成三级任务：

- 任务1 用 ms-swift 的 **grounding 数据格式**（`objects` 字段 + `<ref-object>/<bbox>`）微调，模型直接输出目标框（norm1000 坐标）；
- 任务2/3 用多任务 QA 微调（同 1.1 阶段二）。

| 优点 | 缺点 |
|---|---|
| 部署最简单，一个模型一次推理 | 密集小目标（远处车辆、信号灯）召回明显低于专用检测器 |
| 无级联误差传递 | 计数易出错；推理慢（自回归输出所有框） |
| ms-swift 原生支持，无需第二套训练框架 | bbox 精度低于回归式检测器 |

**适用场景**：demo 验证、标注辅助、对任务1精度要求不高时。**生产环境建议用 1.1。**

### 1.3 增强方案（三模型：加开放词汇检测器兜底长尾）

在 1.1 基础上增加一路**开放词汇检测器**（Grounding DINO / DINO-X / Rex-Omni，零样本推理，无需训练）：

- 闭集 YOLO 负责高频类别（快，路侧实时）；
- 开放词汇检测器按需检长尾目标：文本提示 "overturned vehicle, fire, smoke, scattered cargo, person lying on road"；
- 两路结果合并后注入 Qwen3-VL。

**注意**：Grounding DINO / Rex-Omni **均不在 ms-swift 支持范围内**，需独立部署（Rex-Omni 用官方 `rex_omni` 包，支持 transformers/vLLM 后端；Grounding DINO 用官方仓库或 MMDetection 版本）。此路线适合中心侧（云端）部署，路侧算力有限时可省略。

**Rex-Omni 的另一个用途**：数据生产阶段做**预标注引擎**（检测任意类别 + 输出框），大幅降低人工标注成本，人工只做复核修正。

---

## 2. 视频输入方案

视频的本质增益：**时序信息**能区分图片无法区分的情况（"车停着"是拥堵排队还是事故停驶；双闪是故障车还是事故车），并能定位事件发生的时间窗（对齐本仓库视频规范的 `start_sec/end_sec`）。

### 2.1 推荐方案（检测跟踪 + 时序触发器 + VLM 片段理解）

```
                ┌───────────────────────────────────┐
 视频流 ───────► │ 阶段一：检测 + 多目标跟踪            │──► 任务1输出（逐帧）：
                │ YOLO11 + ByteTrack / BoT-SORT      │    [{track_id, label, bbox}, ...]
                └──────────────┬────────────────────┘
                               │ 轨迹（每目标的位置-时间序列）
                               ▼
                ┌───────────────────────────────────┐
                │ 阶段二：时序触发器（规则 + 轻量统计）  │
                │ · 急减速/急转向/轨迹交叉(碰撞候选)     │──► 正常片段：直接放行
                │ · 长时间停驶、逆行、行人闯入           │    （不送 VLM，省算力）
                │ · 平均车速骤降+密度上升(拥堵候选)      │
                └──────────────┬────────────────────┘
                               │ 异常候选片段（前后各 N 秒，抽 8–32 帧）
                               ▼
                ┌───────────────────────────────────┐
                │ 阶段三：Qwen3-VL 视频理解             │──► 任务2输出：现象 + 时间窗
                │ (ms-swift 视频 SFT，任务A–D)         │    {"现象":"侧翻","start_sec":12,...}
                │ 输入：抽帧序列 + 轨迹摘要文本          │──► 任务3输出：事件分类 + 依据
                └──────────────┬────────────────────┘    + collision_moment 时间窗
                               ▼
                ┌───────────────────────────────────┐
                │ 阶段四(可选)：部署二分类过滤器         │──► 最终告警（是否推送人工）
                │ 任务E：事故/非事故 + 置信度           │
                └───────────────────────────────────┘
```

**各阶段说明：**

| 阶段 | 模型/方法 | 训练工具链 | 作用 |
|---|---|---|---|
| 检测+跟踪 | YOLO11 + ByteTrack（Ultralytics 内置）或 BoT-SORT | Ultralytics / MMDetection | 任务1；产出轨迹供触发器使用 |
| 时序触发器 | 规则引擎（速度/停驶时长/轨迹交叉/密度阈值），可选轻量时序分类器 | 无需训练或 sklearn/轻量模型 | 过滤 95%+ 正常片段，只把可疑片段送 VLM；**这是控制算力成本的关键** |
| 视频 VLM | Qwen3-VL-Instruct（视频输入，抽帧 1–2 fps） | **ms-swift**（视频多任务 SFT + 可选 GRPO） | 任务2/3；输出现象、事件、时间窗（对齐 `event_start_sec/event_end_sec`、`collision_moment_*`） |
| 二分类过滤 | 任务E 微调的小 VLM（如 Qwen3-VL-2B）或直接复用阶段三输出 | ms-swift | 部署侧最终告警决策，压误报 |

**轨迹摘要注入 VLM 的示例 prompt：**

```text
跟踪摘要：车辆#3 在 t=10.2s 急减速(32→0 km/h)后停驶至片段结束；车辆#5 在 t=10.5s 轨迹与#3交叉后停驶；
后方车流 t=15s 起平均速度降至 8 km/h。信号灯状态：绿。
请结合视频帧判断：1) 是否发生碰撞及碰撞时间窗；2) 现象（侧翻/起火/散落物等）；3) 事件类型与依据。
```

轨迹摘要把"图片看不出来的动力学信息"显式交给 VLM，直接治理两类经典误报：

- **拥堵误报**：轨迹显示均匀减速、无交叉 → 拥堵；
- **双闪/故障车误报**：单车缓慢靠边停驶、无碰撞轨迹 → 故障，非事故。

### 2.2 简化方案（定时抽帧 + 图片方案复用）

不做跟踪，每隔 T 秒抽 1 帧走 **1.1 图片方案**，再加一层简单的时序平滑（连续 K 帧判定为事故才告警）。

| 优点 | 缺点 |
|---|---|
| 复用图片全套模型，零额外开发 | 丢失动力学信息，"事故 vs 拥堵"仍主要靠单帧证据 |
| 算力最省 | 无法输出精确碰撞时间窗；瞬态现象（碰撞瞬间）可能漏 |

**适用场景**：快速上线的一期版本；后续平滑升级到 2.1。

### 2.3 端到端方案（Qwen3-VL 直接吃视频）

把整段视频（抽帧）直接送 Qwen3-VL，一次输出任务1/2/3 全部结果（本仓库视频标注规范即为此设计的训练真值）。

- 优点：架构最简，时间窗、现象、事件一次输出，与标注规范完全同构；
- 缺点：算力大（每路视频持续占用 VLM），任务1 的逐帧检测精度不足；**不适合多路实时监控**，适合离线复核、案件回溯、标注辅助。

---

## 3. 两套方案对照总表

| | 图片输入（推荐 1.1） | 视频输入（推荐 2.1） |
|---|---|---|
| 任务1 目标标注 | YOLO11/RT-DETR 单帧检测 | YOLO11 + ByteTrack 检测跟踪 |
| 任务2 现象分析 | Qwen3-VL（原图+检测框注入） | Qwen3-VL 视频片段（抽帧+轨迹摘要注入） |
| 任务3 事件判定 | Qwen3-VL 多任务头（任务D 对比推理） | Qwen3-VL + 时序触发器联合判定 |
| 误报治理手段 | 证据字段（任务B）+ 困难负样本 | 轨迹动力学 + 证据字段双保险 |
| 事件时间定位 | 不适用 | `start_sec/end_sec` + 碰撞瞬间窗 |
| VLM 训练框架 | ms-swift（LoRA SFT，可选 GRPO） | 同左（视频多任务 SFT） |
| 检测器训练框架 | Ultralytics / MMDetection | 同左 |
| 长尾兜底（可选） | Grounding DINO / Rex-Omni 零样本 | 同左（仅中心侧） |

---

## 4. 与本仓库资产的对接

1. **训练数据**：`accident_annotation_spec_video.md` / `accident_annotation_spec_image.md` 的结构化 JSON 是唯一真源：
   - `participants[].roi_box`、`accident_area_box` → 检测器训练数据（转 COCO/YOLO 格式）；
   - 属性/证据/时间窗字段 → 脚本派生任务 A–E 的 QA 对，供 ms-swift 微调 Qwen3-VL；
2. **训练方案**：VLM 部分直接执行 `accident_detection_training_plan.md`（LoRA、两阶段、GRPO、任务 E 二分类）；
3. **预标注提效**：Rex-Omni / Grounding DINO 零样本预标 bbox → 人工复核 → 同时喂检测器与 VLM 两条训练线；
4. **框架分工**：ms-swift 只负责 VLM（Qwen3-VL）训练与部署；检测器用 Ultralytics/MMDetection 独立训练——两者数据同源、格式互转即可，不必强行把检测器塞进 ms-swift。

## 5. 落地顺序建议

1. **第一步（最小闭环）**：图片简化方案 1.2（单 Qwen3-VL）验证任务 2/3 的可行性，同步启动检测器数据标注；
2. **第二步（图片生产版）**：切换到 1.1 两阶段，检测框注入 prompt，用困难负样本（拥堵/双闪/故障车）压误报；
3. **第三步（视频一期）**：2.2 定时抽帧 + 时序平滑，复用图片模型快速覆盖视频流；
4. **第四步（视频完全体）**：补齐跟踪与触发器，升级到 2.1，输出事件时间窗，接入任务 E 告警过滤。
